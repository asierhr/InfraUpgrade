# InfraUpgrade

InfraUpgrade is an experimental CLI for inspecting and safely upgrading
Terraform projects.

## Commands

Scan the current directory:

```powershell
go run ./cmd/Infraupgrade scan .
```

Show the development version:

```powershell
go run ./cmd/Infraupgrade version
```
